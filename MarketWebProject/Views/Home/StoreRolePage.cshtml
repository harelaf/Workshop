@model MainModel
@using MarketWebProject.DTO
@{
    if (Model.IsGuest)
        Layout = "~/Views/Shared/_LayoutGuest.cshtml";
    else if (Model.IsLoggedIn)
        Layout = "~/Views/Shared/_LayoutLoggedIn.cshtml";
    else
        Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewData["Title"] = "Item Editable Page";
}
<div class="text-center">
    <h1 class="display-4">Store @((ViewData["storename"] as string)) Roles Page</h1>
    <br />
</div>
 <h4>Founder</h4>
 <h5>@((ViewData["founder"] as StoreFounderDTO).Username)</h5>
 <br />
 <h4>Owners</h4>
    @if ((ViewData["owners"] as List<StoreOwnerDTO>).Count > 0)
    {
        <table style="width:50%" class="table">
            <tr>
                <th>Owner User Name</th>
                <th>Appointer User Name</th>
            </tr>
            @foreach (StoreOwnerDTO owner in (ViewData["owners"] as List<StoreOwnerDTO>))
            {
                <tr>
                    <th>@owner.Username</th>
                    <th>@owner.Appointer</th>
                </tr>
            }
        </table>
    }
    else
    {
        <h4>store has no owners</h4>
    }
<br />
 <h4>Managers</h4>
    @if ((ViewData["managers"] as List<StoreManagerDTO>).Count > 0)
    {
        <table style="width:50%" class="table">
            <tr>
                <th>Manager User Name</th>
                <th>Appointer User Name</th>
            </tr>
            @foreach (StoreManagerDTO manager in (ViewData["managers"] as List<StoreManagerDTO>))
            {
                <tr>
                    <th>@manager.Username</th>
                    <th>@manager.Appointer</th>
                </tr>
            }
        </table>
    }
    else
    {
        <h4>store has no managers</h4>
    }